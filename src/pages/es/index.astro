---
import Navbar from "../../components/Navbar.astro";
import Hero from "../../components/Hero.astro";
import About from "../../components/about.astro";
import Projects from "../../components/Projects.astro";
import Footer from "../../components/Footer.astro";
import Layout from "../../layouts/Layout.astro";
import { getLangFromURl, useTranslations } from "../../i18n/utils";
const lang = getLangFromURl(Astro.url);
const t = useTranslations(lang);
---

<Layout>
  <Navbar
    resumeFile={t("resume-file")}
    hire={t("hire")}
    language={t("language")}
    pageLanguage={t("pageLanguage")}
    padding="px-2"
  />
  <Hero name={t("name")} imgAlt={t("imgAlt")} />
  <About>
    Amante del rock <span class="font-serif">🎸</span> y techie <span
      class="font-serif">👨‍💻</span
    >. Me gustan los diseños mininalistas y limpios, cómodo para la mayoría de
    usuarios, e increibles para sus ojos<span class="font-serif">😌👀.</span> Quiero
    contribuir al mundo! Checa mi futuro proyecto: <a
      class="text-teal-500 hover:text-stone-500 transition-colors duration-100 ease-in-out"
      href="http://zeta.vercel.app"
      target="_blank"
      rel="noopener noreferrer">zeta app</a
    > la red social para desarrolladores!
  </About>

  <Projects projectsTitle={t("projects-title")} view={t("view")} />
  <Footer
    links={t("links")}
    link1={t("link1")}
    link2={t("link2")}
    socials={t("socials")}
    contact={t("contact")}
  />
</Layout>
